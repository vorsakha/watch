# Source Strategy

Use this order when obtaining script text:

1. IMSDb (`imsdb.com`)
2. Anime fallback (`transcribedanimescripts.tumblr.com`)
3. Generic transcript URL from user/tool input
4. Subtitle fallback (`subtitlecat.com`)

Use `web_fetch` first when possible, then tool HTTP fallback.
